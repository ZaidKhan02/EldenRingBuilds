{/*import express from 'express';
//importing controllers and middleware

const router = express.Router();

router.route('/builds/:buildId').get(getRatingsByBuild).post(submitRatingForBuild);
router.route('/:ratingId').put(updateRating).delete(deleteRating);

export default router

//i will need middleware for authentication authMiddleware for creating, updating, deleting comments
*/}